package env

// goosDarwin is the GOOS value for macOS, used to avoid goconst linter warnings.
const goosDarwin = "darwin"

// goosLinux is the GOOS value for Linux, used to avoid goconst linter warnings.
const goosLinux = "linux"
